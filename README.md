# The Zane Website

The documentation is downloaded from [`zane-lang/docs`](https://github.com/zane-lang/docs)
before starting the development server or building the site. To refresh it manually, run:

```sh
npm run sync:docs
```

Set `ZANE_DOCS_REF` to fetch a branch, tag, or commit other than `main`.
